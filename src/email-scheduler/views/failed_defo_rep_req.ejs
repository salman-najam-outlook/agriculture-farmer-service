<!DOCTYPE html>
<html>
<head>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 1200px;
      margin: 20px auto;
      padding: 20px;
    }
    .header {
      margin-bottom: 20px;
    }
    .content {
      margin-bottom: 30px;
    }
    .note {
      color: #666;
      font-style: italic;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin: 20px 0;
    }
    th, td {
      padding: 10px;
      border: 1px solid #ddd;
      text-align: left;
    }
    th {
      background-color: #f5f5f5;
      font-weight: bold;
    }
    tr:nth-child(even) {
      background-color: #f9f9f9;
    }
    .client-row {
      background-color: #e6f3ff;
      font-weight: bold;
    }
    .issue-row {
      background-color: #ffffff;
    }
    .issue-cell {
      padding-left: 30px;
    }
    .region-cell {
      max-width: 300px;
      word-wrap: break-word;
    }
  </style>
</head>
<body>
  <div class="header">
    <h2>Deforestation Job Report</h2>
    <p>Generated on: <%= date %></p>
  </div>
  
  <div class="content">
    <h3>Summary of Issues</h3>
    <table>
      <thead>
        <tr>
          <th>Client</th>
          <th>Total Farms</th>
          <th>Issues</th>
          <th>Regions</th>
        </tr>
      </thead>
      <tbody>
        <% summaryData.forEach(function(client) { %>
          <tr class="client-row">
            <td><%= client.client %></td>
            <td><%= client.totalFarms %></td>
            <td colspan="2"></td>
          </tr>
          <% client.issues.forEach(function(issue) { %>
            <tr class="issue-row">
              <td></td>
              <td><%= issue.count %></td>
              <td class="issue-cell"><%= issue.issue %></td>
              <td class="region-cell"><%= issue.regions %></td>
            </tr>
          <% }); %>
        <% }); %>
      </tbody>
    </table>

    <p>Please find attached the CSV file containing the detailed report of failed deforestation detection jobs. The report includes information about affected farms, users, and organizations.</p>
    
    <p class="note">For detailed information, please review the attached CSV file: <%= filename %></p>
  </div>
</body>
</html>
